language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - hhvm

services:
  - mysql

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS test;'

before_script:
  - composer install --dev --prefer-source --no-interaction